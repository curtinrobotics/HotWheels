# Temporary stale roll-up test

Disposable marker used only to trigger the managed ancestor reconciliation test.

This second commit triggers the idempotence check without changing the pull request body.
